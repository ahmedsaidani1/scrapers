# System Architecture

Visual overview of how the solar equipment scraping system works.

## System Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     NIGHTLY CRON JOB                        │
│                    (2:00 AM Daily)                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              run_all_scrapers.sh                            │
│         (Batch Execution Script)                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ├──────────────┬──────────────┬──────────┐
                     ▼              ▼              ▼          ▼
              ┌──────────┐   ┌──────────┐   ┌──────────┐
              │Scraper 1 │   │Scraper 2 │   │Scraper N │  ...
              └────┬─────┘   └────┬─────┘   └────┬─────┘
                   │              │              │
                   └──────────────┴──────────────┘
                                  │
                                  ▼
                   ┌──────────────────────────────┐
                   │      BaseScraper Class       │
                   │  (Common Functionality)      │
                   │  - Retry Logic               │
                   │  - Rate Limiting             │
                   │  - Error Handling            │
                   │  - Logging                   │
                   └──────────┬───────────────────┘
                              │
                ┌─────────────┴─────────────┐
                ▼                           ▼
    ┌───────────────────┐       ┌──────────────────┐
    │  Target Website   │       │   CSV Output     │
    │  (Solar Products) │       │   data/*.csv     │
    └───────────────────┘       └────────┬─────────┘
                                         │
                                         ▼
                              ┌──────────────────────┐
                              │ Google Sheets Helper │
                              │  (Auto Push)         │
                              └──────────┬───────────┘
                                         │
                                         ▼
                              ┌──────────────────────┐
                              │   Google Sheets      │
                              │  (Data Storage)      │
                              └──────────┬───────────┘
                                         │
                                         ▼
                              ┌──────────────────────┐
                              │     Power BI         │
                              │  (Visualization)     │
                              └──────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    CONFIGURATION LAYER                       │
├─────────────────────────────────────────────────────────────┤
│  config.py                                                   │
│  - Website URLs                                              │
│  - Google Sheet IDs                                          │
│  - Scraping Settings                                         │
│  - User Agents                                               │
│  - Rate Limits                                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    FRAMEWORK LAYER                           │
├─────────────────────────────────────────────────────────────┤
│  base_scraper.py          │  google_sheets_helper.py        │
│  - HTTP Requests          │  - Authentication               │
│  - HTML Parsing           │  - Sheet Updates                │
│  - Error Handling         │  - Data Push                    │
│  - Retry Logic            │  - Error Handling               │
│  - Rate Limiting          │                                 │
│  - Logging                │                                 │
│  - CSV Output             │                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   SCRAPER LAYER                              │
├─────────────────────────────────────────────────────────────┤
│  Individual Scrapers (inherit from BaseScraper)             │
│                                                              │
│  scraper1.py  │  scraper2.py  │  scraper3.py  │  ...       │
│                                                              │
│  Each implements:                                            │
│  - get_product_urls()  → List of URLs to scrape             │
│  - scrape_product()    → Extract data from page             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   AUTOMATION LAYER                           │
├─────────────────────────────────────────────────────────────┤
│  run_all_scrapers.sh      │  setup_cron.sh                  │
│  - Sequential Execution   │  - Cron Configuration           │
│  - Error Tracking         │  - Schedule Setup               │
│  - Master Logging         │                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    OUTPUT LAYER                              │
├─────────────────────────────────────────────────────────────┤
│  data/                    │  logs/                          │
│  - CSV files              │  - Individual scraper logs      │
│  - Standardized format    │  - Master batch logs            │
│  - One per scraper        │  - Cron execution logs          │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

```
1. DISCOVERY PHASE
   ┌──────────────────┐
   │  get_product_    │
   │  urls()          │
   └────────┬─────────┘
            │
            ├─→ Sitemap.xml parsing
            ├─→ Category page scraping
            └─→ API endpoint calls
            │
            ▼
   ┌──────────────────┐
   │  List of URLs    │
   └────────┬─────────┘

2. EXTRACTION PHASE
            │
            ▼
   ┌──────────────────┐
   │  For each URL:   │
   │  scrape_product()│
   └────────┬─────────┘
            │
            ├─→ HTTP Request (with retry)
            ├─→ HTML Parsing (BeautifulSoup)
            ├─→ Data Extraction (CSS selectors)
            └─→ Data Validation
            │
            ▼
   ┌──────────────────┐
   │  Product Data    │
   │  Dictionary      │
   └────────┬─────────┘

3. STORAGE PHASE
            │
            ├─────────────────┐
            ▼                 ▼
   ┌──────────────┐  ┌──────────────────┐
   │  CSV File    │  │  Google Sheets   │
   │  (Local)     │  │  (Cloud)         │
   └──────────────┘  └──────────────────┘
```

## Class Hierarchy

```
┌─────────────────────────────────────┐
│         BaseScraper (ABC)           │
│  ─────────────────────────────────  │
│  + __init__(scraper_name)           │
│  + make_request(url)                │
│  + parse_html(html)                 │
│  + save_product(data)               │
│  + run()                            │
│  ─────────────────────────────────  │
│  # get_product_urls() [abstract]    │
│  # scrape_product(url) [abstract]   │
└──────────────┬──────────────────────┘
               │
               │ inherits
               │
    ┌──────────┴──────────┬──────────────┐
    │                     │              │
    ▼                     ▼              ▼
┌─────────┐         ┌─────────┐    ┌─────────┐
│Scraper1 │         │Scraper2 │    │ScraperN │
│         │         │         │    │         │
│implements:        │implements:   │implements:
│- get_product_urls │- get_product_urls
│- scrape_product   │- scrape_product
└─────────┘         └─────────┘    └─────────┘
```

## Request Flow with Error Handling

```
User/Cron triggers scraper
         │
         ▼
┌────────────────────┐
│  Scraper.run()     │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ get_product_urls() │
└────────┬───────────┘
         │
         ▼
┌────────────────────────────────┐
│  For each URL:                 │
│                                │
│  ┌──────────────────────────┐ │
│  │ make_request(url)        │ │
│  │   │                      │ │
│  │   ├─→ Try request        │ │
│  │   │   ├─ Success → Return│ │
│  │   │   └─ Fail ↓          │ │
│  │   │                      │ │
│  │   ├─→ Retry (attempt 2)  │ │
│  │   │   ├─ Success → Return│ │
│  │   │   └─ Fail ↓          │ │
│  │   │                      │ │
│  │   ├─→ Retry (attempt 3)  │ │
│  │   │   ├─ Success → Return│ │
│  │   │   └─ Fail ↓          │ │
│  │   │                      │ │
│  │   └─→ Log error, return  │ │
│  │       None               │ │
│  └──────────────────────────┘ │
│         │                      │
│         ▼                      │
│  ┌──────────────────────────┐ │
│  │ scrape_product(url)      │ │
│  │   │                      │ │
│  │   ├─→ Parse HTML         │ │
│  │   ├─→ Extract data       │ │
│  │   ├─→ Validate data      │ │
│  │   └─→ Return dict/None   │ │
│  └──────────────────────────┘ │
│         │                      │
│         ▼                      │
│  ┌──────────────────────────┐ │
│  │ save_product(data)       │ │
│  │   │                      │ │
│  │   └─→ Write to CSV       │ │
│  └──────────────────────────┘ │
│         │                      │
│         ▼                      │
│  ┌──────────────────────────┐ │
│  │ Random delay (1-3s)      │ │
│  └──────────────────────────┘ │
└────────────────────────────────┘
         │
         ▼
┌────────────────────┐
│ push_data()        │
│ (if --push-to-     │
│  sheets flag)      │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Google Sheets      │
│ Updated            │
└────────────────────┘
```

## File System Layout

```
/root/solar-scrapers/
│
├── Core Framework
│   ├── config.py                 # Configuration
│   ├── base_scraper.py          # Base class
│   ├── google_sheets_helper.py  # Sheets integration
│   └── scraper_template.py      # Template
│
├── Scrapers (you create these)
│   ├── scraper1.py
│   ├── scraper2.py
│   ├── scraper3.py
│   └── ...
│
├── Automation
│   ├── run_all_scrapers.sh      # Batch runner
│   └── setup_cron.sh            # Cron setup
│
├── Testing & Docs
│   ├── test_framework.py        # Tests
│   ├── sample_scraper.py        # Example
│   ├── README.md                # Full docs
│   ├── QUICKSTART.md            # Quick guide
│   ├── DEPLOYMENT.md            # Deploy guide
│   ├── SCRAPER_CHECKLIST.md     # Checklist
│   ├── PROJECT_SUMMARY.md       # Summary
│   └── ARCHITECTURE.md          # This file
│
├── Configuration
│   ├── requirements.txt         # Dependencies
│   └── .gitignore              # Git config
│
├── Credentials (you add)
│   ├── credentials.json         # Google creds
│   └── README.md               # Creds guide
│
├── Data (auto-created)
│   ├── scraper1.csv
│   ├── scraper2.csv
│   └── ...
│
└── Logs (auto-created)
    ├── scraper1.log
    ├── scraper2.log
    ├── master_YYYYMMDD_HHMMSS.log
    └── cron.log
```

## Execution Timeline

```
Daily Execution (2:00 AM)
│
├─ 02:00:00  Cron triggers run_all_scrapers.sh
│
├─ 02:00:01  Scraper 1 starts
│   ├─ Get product URLs (30s)
│   ├─ Scrape products (5-10 min)
│   └─ Push to Google Sheets (10s)
│
├─ 02:10:00  Scraper 2 starts
│   ├─ Get product URLs (30s)
│   ├─ Scrape products (5-10 min)
│   └─ Push to Google Sheets (10s)
│
├─ 02:20:00  Scraper 3 starts
│   └─ ...
│
├─ ...
│
└─ 03:00:00  All scrapers complete
    ├─ Master log written
    ├─ Summary generated
    └─ Exit
```

## Technology Stack

```
┌─────────────────────────────────────┐
│         Application Layer           │
│  Python 3.8+ (Scraping Logic)       │
└─────────────────┬───────────────────┘
                  │
┌─────────────────┴───────────────────┐
│         Framework Layer             │
│  - requests (HTTP)                  │
│  - BeautifulSoup4 (HTML parsing)    │
│  - gspread (Google Sheets)          │
│  - oauth2client (Authentication)    │
└─────────────────┬───────────────────┘
                  │
┌─────────────────┴───────────────────┐
│         System Layer                │
│  - Ubuntu 20.04 (OS)                │
│  - Cron (Scheduling)                │
│  - Bash (Automation)                │
└─────────────────┬───────────────────┘
                  │
┌─────────────────┴───────────────────┐
│         External Services           │
│  - Target Websites (Data Source)    │
│  - Google Sheets API (Storage)      │
│  - Power BI (Visualization)         │
└─────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────┐
│         Credentials Layer           │
│  credentials/credentials.json       │
│  - Not in git (.gitignore)          │
│  - File permissions: 600            │
│  - Service account only             │
└─────────────────┬───────────────────┘
                  │
                  │ OAuth2
                  ▼
┌─────────────────────────────────────┐
│      Google Cloud Platform          │
│  - Service Account                  │
│  - Limited permissions              │
│  - API key rotation                 │
└─────────────────┬───────────────────┘
                  │
                  │ HTTPS
                  ▼
┌─────────────────────────────────────┐
│         Google Sheets               │
│  - Shared with service account      │
│  - Editor permissions only          │
└─────────────────────────────────────┘
```

## Scalability Considerations

```
Current Setup (10 scrapers):
├─ Execution time: ~30-45 minutes
├─ Products: ~1000 per run
├─ Storage: ~10MB CSV + Google Sheets
└─ Server load: Low

Scaling to 50 scrapers:
├─ Execution time: ~2-3 hours
├─ Products: ~5000 per run
├─ Storage: ~50MB CSV + Google Sheets
├─ Considerations:
│   ├─ Parallel execution (multiple processes)
│   ├─ Database instead of CSV
│   ├─ Distributed scraping
│   └─ Load balancing

Scaling to 100+ scrapers:
├─ Consider:
│   ├─ Scrapy framework
│   ├─ Distributed task queue (Celery)
│   ├─ Multiple servers
│   ├─ Database (PostgreSQL/MongoDB)
│   └─ Monitoring system (Prometheus)
```

## Monitoring & Observability

```
┌─────────────────────────────────────┐
│            Logs Layer               │
│  logs/                              │
│  ├─ Individual scraper logs         │
│  ├─ Master batch logs               │
│  └─ Cron execution logs             │
└─────────────────┬───────────────────┘
                  │
                  │ Analysis
                  ▼
┌─────────────────────────────────────┐
│         Metrics to Monitor          │
│  - Success rate per scraper         │
│  - Execution time                   │
│  - Products scraped                 │
│  - Error frequency                  │
│  - Google Sheets push success       │
└─────────────────────────────────────┘
```

## Key Design Principles

1. **Separation of Concerns**
   - Configuration separate from code
   - Framework separate from scrapers
   - Data separate from logic

2. **DRY (Don't Repeat Yourself)**
   - Common functionality in base class
   - Reusable helper functions
   - Template for new scrapers

3. **Fail Gracefully**
   - Retry logic for transient failures
   - Continue on individual failures
   - Comprehensive error logging

4. **Be Respectful**
   - Rate limiting
   - User agent rotation
   - Respect robots.txt

5. **Easy to Maintain**
   - Clear code structure
   - Comprehensive documentation
   - Simple deployment

This architecture provides a solid foundation for your solar equipment price scraping system! 🚀
